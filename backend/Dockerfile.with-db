# Backend Dockerfile - Alternative version with auto-migration
# Use this with docker-compose-with-db.yml for embedded PostgreSQL

FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --only=production

# Copy application files
COPY . .

# Expose port
EXPOSE 3000

# Run migrations and start server (only use with embedded database)
CMD ["sh", "-c", "npx knex migrate:latest && npm start"]
